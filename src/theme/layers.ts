const loadingOverlay = 800
const notifications = 700
const modalHeader = 600
const modal = 500
const colorPickerPopover = 400
const overlay = 300
const canvasElement = 200
const canvasApp = 100

export {
  loadingOverlay,
  notifications,
  modalHeader,
  modal,
  colorPickerPopover,
  overlay,
  canvasElement,
  canvasApp,
}
